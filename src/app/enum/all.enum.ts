/* eslint-disable @typescript-eslint/naming-convention */

export enum EStorage {
  EXPIRE = 'expires_at',
  LOGIN = 'login'
}

export enum EAccountPages {
  REGISTER = 'REGISTER',
  LOGIN = 'LOGIN',
  FORGET = 'FORGET',
}
